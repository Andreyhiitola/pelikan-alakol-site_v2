# Пеликан Алаколь - Руководство по проекту

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Структура проекта](#структура-проекта)
3. [Получение Google Maps API ключа](#получение-google-maps-api-ключа)
4. [Настройка карты](#настройка-карты)
5. [Настройка SEO](#настройка-seo)
6. [Интеграция MaxiBooking](#интеграция-maxibooking)
7. [Модульная структура карты](#модульная-структура-карты)
8. [Развертывание](#развертывание)
9. [Поддержка и обновления](#поддержка-и-обновления)

---

## 🎯 Обзор проекта

**Парк-отель "Пеликан Алаколь"** - одностраничный сайт для базы отдыха на озере Алаколь.

### Основные функции:
- 🏨 Каталог номеров и инфраструктуры
- 🗺️ Интерактивная карта с маршрутами
- 🌤️ Прогноз погоды
- 📞 Контакты с интеграцией мессенджеров
- 🎫 Онлайн бронирование через MaxiBooking
- 📱 Адаптивный дизайн (мобильные + десктоп)
- 🌓 Темная тема

---

## 📁 Структура проекта

```
pelikan-alakol-site/
│
├── index.html              # Главная страница
│
├── css/
│   └── infrastructure.css  # Стили инфраструктуры
│
├── js/
│   ├── accommodation.js    # Номера
│   ├── activities.js       # Активности
│   ├── contacts.js         # Контакты
│   ├── gallery.js          # Галерея
│   ├── infrastructure.js   # Инфраструктура
│   ├── main.js             # Главная логика
│   ├── menu.js             # Меню
│   ├── offers.js           # Предложения
│   ├── reviews.js          # Отзывы
│   └── map.js              # Карта (опционально)
│
├── contacts.json           # Данные контактов (в корне!)
├── rooms.json              # Данные номеров (в корне!)
├── infrastructure.json     # Данные инфраструктуры (в корне!)
│
└── images/                 # Изображения
```

**⚠️ Важно:** JSON файлы лежат в **корне проекта**, а не в папке `data/`!

---

## 🗺️ Получение Google Maps API ключа

### Шаг 1: Создание Google Cloud проекта

1. **Перейдите в Google Cloud Console:**
   - https://console.cloud.google.com/

2. **Войдите в аккаунт Google**
   - Используйте существующий или создайте новый

3. **Создайте новый проект:**
   - Нажмите на выпадающий список проектов (вверху страницы)
   - Выберите **"Новый проект"** (New Project)
   - Введите название: `Pelican Alakol Site`
   - Нажмите **"Создать"** (Create)

### Шаг 2: Включение Maps JavaScript API

1. **Откройте библиотеку API:**
   - В боковом меню выберите: **APIs & Services → Library**
   - Или перейдите: https://console.cloud.google.com/apis/library

2. **Найдите Maps JavaScript API:**
   - В поиске введите: `Maps JavaScript API`
   - Выберите **Maps JavaScript API** из результатов

3. **Включите API:**
   - Нажмите кнопку **"Enable"** (Включить)
   - Дождитесь активации (обычно 1-2 минуты)

### Шаг 3: Создание API ключа

1. **Перейдите в раздел учетных данных:**
   - Боковое меню: **APIs & Services → Credentials**
   - Или: https://console.cloud.google.com/apis/credentials

2. **Создайте новый ключ:**
   - Нажмите **"+ Create Credentials"** (Создать учетные данные)
   - Выберите **"API key"**
   - Ключ будет создан автоматически

3. **Скопируйте ключ:**
   - Появится окно с вашим API ключом
   - Формат: `AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
   - **Скопируйте и сохраните его в безопасном месте!**

### Шаг 4: Настройка ограничений (ВАЖНО!)

**⚠️ Без ограничений ваш ключ может быть украден и использован злоумышленниками!**

1. **Откройте настройки ключа:**
   - В списке ключей нажмите на созданный ключ
   - Или нажмите на иконку редактирования (карандаш)

2. **Настройте ограничения приложения:**
   - Найдите раздел **"Application restrictions"**
   - Выберите **"HTTP referrers (web sites)"**
   - Нажмите **"Add an item"**
   - Добавьте разрешенные домены:

   ```
   https://pelican-alacol.ru/*
   https://*.pelican-alacol.ru/*
   http://localhost/*
   http://127.0.0.1/*
   ```

   **Замените `pelican-alacol.ru` на ваш реальный домен!**

3. **Настройте ограничения API:**
   - Найдите раздел **"API restrictions"**
   - Выберите **"Restrict key"**
   - В списке отметьте:
     - ✅ **Maps JavaScript API**
   - Снимите все остальные галочки

4. **Сохраните изменения:**
   - Нажмите **"Save"** внизу страницы

### Шаг 5: Настройка биллинга

**⚠️ Без включенного биллинга API не будет работать!**

1. **Перейдите в раздел биллинга:**
   - Боковое меню: **Billing**
   - Или: https://console.cloud.google.com/billing

2. **Привяжите платежный аккаунт:**
   - Если аккаунт не создан — нажмите **"Create account"**
   - Введите данные банковской карты
   - Подтвердите создание

3. **Бесплатный кредит:**
   - Google предоставляет **$200/месяц бесплатно**
   - Этого хватает на **~28,000 загрузок карты**
   - Для небольшого сайта этого достаточно

4. **Настройка бюджетных лимитов (опционально):**
   - **Billing → Budgets & alerts**
   - Нажмите **"Create budget"**
   - Установите лимит (например, $50)
   - Настройте email-уведомления при 50%, 90%, 100%

---

## ⚙️ Настройка карты

### Вставка API ключа в код

1. **Откройте `index.html`**

2. **Найдите строку (~строка 17):**
   ```html
   <script src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&callback=initMap" async defer></script>
   ```

3. **Замените `YOUR_API_KEY` на ваш реальный ключ:**
   ```html
   <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxx&callback=initMap" async defer></script>
   ```

4. **Сохраните файл**

### Проверка работы карты

1. **Откройте `index.html` в браузере**

2. **Откройте консоль разработчика (F12)**

3. **Проверьте ошибки:**
   - ✅ **Нет ошибок** — карта работает!
   - ❌ **InvalidKeyMapError** — проверьте правильность ключа
   - ❌ **RefererNotAllowedMapError** — добавьте домен в ограничения
   - ❌ **BillingNotEnabledMapError** — включите биллинг

### Замена на бесплатную карту (опционально)

Если не хотите использовать Google Maps, можете заменить на бесплатный OpenStreetMap:

1. **В `<head>` замените:**
   ```html
   <!-- Удалите Google Maps API -->
   <!-- <script src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&callback=initMap" async defer></script> -->

   <!-- Добавьте Leaflet (OpenStreetMap) -->
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/leaflet.min.css" />
   <script src="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/leaflet.min.js"></script>
   ```

2. **Замените функцию `initMap()` в блоке `<script>`:**
   ```javascript
   function initMap() {
     if (mapInitialized) return;
     
     const map = L.map('map').setView([LATITUDE, LONGITUDE], 11);
     
     L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
       attribution: '© OpenStreetMap contributors',
       maxZoom: 18
     }).addTo(map);
     
     L.marker([LATITUDE, LONGITUDE])
       .bindPopup('🌲 Пеликан Алаколь')
       .addTo(map)
       .openPopup();
       
     mapInitialized = true;
   }
   ```

---

## 🔍 Настройка SEO

### Обновление мета-тегов

Откройте `index.html` и обновите следующие параметры в секции `<head>`:

1. **Основные мета-теги:**
   ```html
   <meta name="description" content="Ваше описание базы отдыха (до 160 символов)">
   <meta name="keywords" content="ваши ключевые слова, через запятую">
   ```

2. **Open Graph (для соцсетей):**
   ```html
   <meta property="og:url" content="https://ваш-домен.com">
   <meta property="og:image" content="https://ваш-домен.com/images/og-image.jpg">
   ```

   **Требования к изображению:**
   - Размер: 1200×630 пикселей
   - Формат: JPG или PNG
   - Вес: до 1 МБ

3. **Canonical URL:**
   ```html
   <link rel="canonical" href="https://ваш-домен.com/">
   ```

4. **Structured Data (Schema.org):**
   ```html
   <script type="application/ld+json">
   {
     "@context": "https://schema.org",
     "@type": "Hotel",
     "name": "Название вашей базы",
     "telephone": "+7-xxx-xxx-xxxx",
     "address": {
       "streetAddress": "ваш адрес",
       "addressLocality": "город",
       "addressCountry": "KZ"
     },
     "geo": {
       "latitude": 45.955,
       "longitude": 81.5714
     }
   }
   </script>
   ```

### Проверка SEO

1. **Google Search Console:**
   - https://search.google.com/search-console
   - Добавьте ваш сайт
   - Проверьте индексацию

2. **Проверка Open Graph:**
   - https://developers.facebook.com/tools/debug/
   - Введите URL вашего сайта
   - Проверьте превью

3. **Structured Data тест:**
   - https://search.google.com/test/rich-results
   - Проверьте корректность Schema.org

---

## 🎫 Интеграция MaxiBooking

### Текущая интеграция

MaxiBooking уже интегрирован в `index.html`:

```html
<!-- Форма поиска -->
<script src="https://pelikanalakol.maxi-booking.ru/management/online/api/file/5e1dfd8c67a07d5cbd33ce5a/load-search-form"></script>

<!-- Результаты -->
<script src="https://pelikanalakol.maxi-booking.ru/management/online/api/file/5e1dfd8c67a07d5cbd33ce5a/load-result"></script>
```

### Как это работает

1. Скрипты MaxiBooking загружаются динамически
2. Виджет встраивается в `#mbh-form-wrapper`
3. Результаты отображаются в `#mbh-results-wrapper`
4. Условия бронирования показаны под виджетом

### Настройка MaxiBooking

Если у вас другой аккаунт MaxiBooking:

1. Войдите в панель MaxiBooking
2. Перейдите в **Настройки → Виджет бронирования**
3. Скопируйте ваши URL для:
   - `load-search-form`
   - `load-result`
4. Замените в `index.html`

---

## 🔧 Модульная структура карты

### Текущая структура

Сейчас вся логика карты встроена в `index.html`.

### Переход на модульную структуру

**Преимущества:**
- Код карты изолирован в `js/map.js`
- Легко менять провайдера карты
- Проще поддерживать и тестировать

**Инструкция:**

1. **Создайте файл `js/map.js`**

2. **Скопируйте содержимое из комментария в `index.html`**
   - В конце файла есть большой блок комментариев
   - Там находится полное содержимое `js/map.js`

3. **В `index.html` раскомментируйте строку:**
   ```html
   <script src="js/map.js"></script>
   ```

4. **Удалите блок `========== КАРТА ==========`** из основного `<script>`

### Смена провайдера карты

После перехода на модульную структуру, для смены карты:

1. **Откройте `js/map.js`**
2. **Замените только функцию `initMap()`**
3. **Остальные функции не трогайте!**

Примеры функций `initMap()` для разных провайдеров есть в комментариях `index.html`.

---

## 🚀 Развертывание

### Вариант 1: GitHub Pages

1. **Создайте репозиторий на GitHub**

2. **Загрузите все файлы проекта**

3. **Настройте GitHub Pages:**
   - Settings → Pages
   - Source: Deploy from a branch
   - Branch: main → / (root)
   - Save

4. **Ваш сайт будет доступен по адресу:**
   ```
   https://ваш-username.github.io/pelikan-alakol-site/
   ```

### Вариант 2: VPS (Docker)

1. **Установите Docker на сервер**

2. **Создайте `Dockerfile`:**
   ```dockerfile
   FROM nginx:alpine
   COPY . /usr/share/nginx/html
   EXPOSE 80
   CMD ["nginx", "-g", "daemon off;"]
   ```

3. **Постройте и запустите:**
   ```bash
   docker build -t pelikan-site .
   docker run -d -p 80:80 pelikan-site
   ```

### Вариант 3: Обычный хостинг

1. **Загрузите все файлы через FTP/SFTP**
2. **Убедитесь, что `index.html` в корне**
3. **Проверьте доступность всех файлов**

---

## 🔄 Поддержка и обновления

### Обновление данных

**Номера и инфраструктура:**
- Редактируйте `rooms.json` (в корне проекта!)
- Редактируйте `infrastructure.json` (в корне проекта!)

**Контакты:**
- Редактируйте `contacts.json` (в корне проекта!)

**Изображения:**
- Добавляйте в папку `images/`
- Обновляйте ссылки в JSON файлах

### Мониторинг Google Maps API

1. **Проверяйте использование:**
   - https://console.cloud.google.com/apis/dashboard
   - Следите за количеством запросов

2. **Настройте бюджетные алерты:**
   - Billing → Budgets & alerts
   - Получайте уведомления при превышении

### Резервное копирование

**Рекомендуется делать бэкап:**
- Всех файлов проекта
- JSON данных (contacts.json, rooms.json, infrastructure.json)
- Изображений

**Частота:** раз в неделю или после крупных изменений.

---

## 📞 Поддержка

### Документация

- **Google Maps API:** https://developers.google.com/maps/documentation/javascript
- **MaxiBooking:** https://maxi-booking.ru/support
- **OpenStreetMap/Leaflet:** https://leafletjs.com/reference.html

### Контакты

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Прочитайте сообщение об ошибке
3. Используйте раздел troubleshooting в документации

---

## ✅ Чеклист запуска

- [ ] Получен Google Maps API ключ
- [ ] Ключ вставлен в `index.html`
- [ ] Настроены ограничения API ключа
- [ ] Включен биллинг в Google Cloud
- [ ] Карта работает без ошибок
- [ ] Обновлены все SEO мета-теги
- [ ] Добавлены реальные изображения
- [ ] Проверена интеграция MaxiBooking
- [ ] Обновлены контакты в `contacts.json` (корень!)
- [ ] Обновлены номера в `rooms.json` (корень!)
- [ ] Обновлена инфраструктура в `infrastructure.json` (корень!)
- [ ] Сайт развернут на хостинге
- [ ] Проверена работа на мобильных устройствах

---

**Удачи с проектом! 🌲 Пеликан Алаколь**
