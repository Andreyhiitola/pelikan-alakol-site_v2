backend:
  name: test-repo
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  - name: "accommodation"
    label: "🏨 Номера (20 шт)"
    files:
      - label: "Номера"
        name: "accommodation"
        file: "accommodation.json"
        fields:
          - label: "Номера"
            widget: "list"
            fields:
              - {label: "ID", name: "id", widget: "string"}
              - {label: "Название", name: "name", widget: "string"}
              - {label: "Цена", name: "priceRange", widget: "string"}
              - {label: "Вместимость", name: "capacity", widget: "number"}
              - {label: "Фото", name: "imageThumb", widget: "image", folder: "accommodation"}
