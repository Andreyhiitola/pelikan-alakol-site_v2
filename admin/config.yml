backend:
  name: test-repo

media_folder: "images"
public_folder: "/images"

collections:
  - name: "accommodation"
    label: "🏨 Номера"
    files:
      - name: "accommodation"
        label: "20 номеров"
        file: "accommodation.json"
        fields:
          - label: "Номера (массив)"
            name: "root"
            widget: "list"
            fields:
              - {label: "ID", name: "id", widget: "string"}
              - {label: "Название", name: "name", widget: "string"}
              - {label: "Цена", name: "priceRange", widget: "string"}
              - {label: "Фото", name: "imageThumb", widget: "image"}
