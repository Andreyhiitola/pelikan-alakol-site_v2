# Журнал изменений

Хронологический лог событий и решений проекта.
Технические детали каждого изменения — в git-истории и CHANGELOG.md.

---

## 2026-04-15

- Создана wiki/ (LLM Wiki по методу Карпати).
- CLAUDE.md дополнен: описание сайтовых JS-модулей, схема payload для `/api/order`, список стоп-файлов.
- Создан STATUS.md.

## 2026-04-09

- Проведён code review `bar.html` и `bar.js`.
- Зафиксированы критические баги: двойная `<form>`, XSS в `showContactModal`, телефон-заглушка.
- Зафиксированы архитектурные проблемы: захардкоженные URL, инлайн CSS, `window.currentOrderText`.
- Создана дорожная карта в памяти Claude (`roadmap.md`).

## 2026-04-09 (ранее в сессии)

- Добавлен Paybox-модал выбора оплаты в `bar.html`.
- Убран `tg.sendData()` — закрывал Mini App до показа платёжного модала.

## 2026-01-18

- Добавлена система отзывов в бот (`pelikan-bot` v2.0).
- Новые эндпоинты: `GET /api/reviews`.
- Страница `reviews.html` + `js/reviews.js` на сайте.

## 2025-12-01

- Базовая система заказов (`bar.html`, `js/bar.js`, `barzakaz.json`).
- Интеграция с Telegram-ботом через `POST /api/order`.
- Генерация PDF-накладных в боте.

## 2025 (ранее)

- Запуск сайта v3-stable с основными разделами.
- Интерактивные карточки инфраструктуры (`infrastructure.json` + `js/infrastructure.js`).
- Галерея, активности, размещение, бронирование, контакты.
